version: '3.8'

services:
  backend:
    build: ./back
    container_name: fastapi_backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./front
    container_name: streamlit_frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend
    restart: always